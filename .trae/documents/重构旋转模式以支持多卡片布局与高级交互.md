我已完成对新需求的分析，将通过重构旋转模式组件来实现您的要求。

### 核心修改点
1.  **布局适配**：
    *   修改卡片宽度计算逻辑，从固定宽度改为基于屏幕宽度的动态计算（`屏幕宽度 / 3.5`），实现横向排列 3-4 个卡片。
    *   相应调整卡片的高度和缩放比例，强化“中央变大”的视觉效果。

2.  **动画与交互重构**：
    *   **统一动画循环**：放弃原有的 `Animated.decay`，采用统一的 `requestAnimationFrame` 循环来驱动所有动画。
    *   **交互逻辑**：
        *   **长按暂停**：手指按下即暂停自动旋转。
        *   **滑动加速**：手指释放后，根据手势速度给予初始速度。
        *   **平滑恢复**：速度将通过物理插值算法（`Lerp`）从手势速度平滑过渡回设定的自动旋转速度，实现无缝衔接。

3.  **UI 优化**：
    *   调整底部控制栏的布局，使其更美观且易于操作。
    *   优化速度滑块的响应范围。

### 执行步骤
1.  **升级 MenuCard 组件**：
    *   修改 `MenuCard.js`，使其支持动态传入 `cardWidth`。
    *   调整缩放插值器 (`inputRange`, `outputRange`)，增强中间放大的效果。
2.  **重写 RotatingModeView 组件**：
    *   将 `RotatingModeView.js` 重构为基于物理状态机（AUTO / DRAGGING / INERTIA）的新实现。
    *   集成新的动画循环逻辑。
    *   更新 UI 布局。
3.  **验证**：
    *   确保修改后的组件直接替换原有逻辑，无需修改主屏幕代码即可生效。
    *   启动项目验证效果。
